run npm install for installing the project 


run docker compose up for making the project running with docker compose


run docker compose down if you want to stop docker compose


the project is running on port 3000 with docker compose !

put the port number in .env file (PORT=3000)

if you want to run the project with mongodb database with cloud or mongo-compass for example just putting the link 
in .env file and excute the command : nodemon server 
